# Crie um script que encerre um processo específico, dado seu PID.
# Dica:
# Use psutil.Process(pid).terminate() para encerrar o processo. Trate exceções como
# psutil.NoSuchProcess caso o PID não exista.
